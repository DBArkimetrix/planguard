repos:

  - repo: local
    hooks:

      - id: planguard-check
        name: PlanGuard checks
        entry: planguard check
        language: system
        pass_filenames: false
        verbose: true
        stages: [manual]
