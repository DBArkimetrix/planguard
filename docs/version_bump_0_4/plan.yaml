plan:
  name: version_bump_0_4
  status: completed
  created: '2026-03-23'
  owner: codex
  priority: high
objective: Update package version metadata so pyproject.toml and planguard/__init__.py
  both report version 0.4
scope:
  included:
  - pyproject.toml
  - planguard
  excluded:
  - unrelated modules
phases:
- name: analysis
  tasks:
  - Analyze current implementation
  - Identify dependencies and risks
- name: implementation
  tasks:
  - Implement changes in safe, testable slices
- name: validation
  tasks:
  - Run tests and review for regressions
risks:
- id: RISK-001
  description: Regression impact on existing behaviour
  severity: medium
  mitigation: Add targeted regression tests before changing code
dependencies:
- id: analysis
  depends_on: []
- id: implementation
  depends_on:
  - analysis
- id: validation
  depends_on:
  - implementation
backlog:
- id: BL-001
  title: Analyze scope, architecture touchpoints, and test impact
  type: analysis
  phase: analysis
  scope:
  - pyproject.toml
  - planguard
  depends_on: []
  deliverables:
  - Impacted modules and dependencies are identified
  - Required tests and rollback considerations are documented
  tests:
  - Identify the regression coverage that must be preserved before coding begins
  done_when: &id001
  - The implementation approach is clear
  - The test strategy covers the affected areas
- id: BL-002
  title: Implement changes in pyproject.toml
  type: implementation
  phase: implementation
  scope:
  - pyproject.toml
  depends_on:
  - BL-001
  deliverables:
  - Code changes in pyproject.toml are implemented in safe, reviewable slices
  tests:
  - Add or update focused regression tests covering pyproject.toml
  done_when:
  - The planned work for pyproject.toml is complete
- id: BL-003
  title: Implement changes in planguard
  type: implementation
  phase: implementation
  scope:
  - planguard
  depends_on:
  - BL-001
  deliverables:
  - Code changes in planguard are implemented in safe, reviewable slices
  tests:
  - Add or update focused regression tests covering planguard
  done_when:
  - The planned work for planguard is complete
- id: BL-004
  title: Run verification, regression checks, and handoff review
  type: validation
  phase: validation
  scope:
  - pyproject.toml
  - planguard
  depends_on:
  - BL-002
  - BL-003
  deliverables:
  - Verification commands pass
  - Handoff notes capture any residual risk or follow-up work
  tests: &id003
  - poetry run pytest tests/ -v
  done_when: &id002
  - All tests pass
  - No regressions in existing functionality
sprints:
- id: SPRINT-01
  name: Discovery and test design
  goal: Confirm scope, architecture impact, and required regression coverage before
    implementation starts.
  backlog_items:
  - BL-001
  focus_paths:
  - planguard
  - pyproject.toml
  exit_criteria: *id001
- id: SPRINT-02
  name: Implementation slice 1
  goal: Deliver a reviewable subset of the planned change set with matching test updates.
  backlog_items:
  - BL-002
  - BL-003
  focus_paths:
  - planguard
  - pyproject.toml
  exit_criteria:
  - The planned work for pyproject.toml is complete
  - The planned work for planguard is complete
- id: SPRINT-03
  name: Verification and handoff
  goal: Prove the change set works, document residual risk, and prepare completion.
  backlog_items:
  - BL-004
  focus_paths:
  - planguard
  - pyproject.toml
  exit_criteria: *id002
done_when:
- All tests pass
- No regressions in existing functionality
verify_commands: *id003
rollback_strategy: git revert to prior commit
test_strategy:
- area: Existing functionality in scope paths
  validation: Confirm no unintended behaviour changes
