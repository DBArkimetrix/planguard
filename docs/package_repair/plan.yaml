plan:
  name: package_repair
  created: 2026-03-11
  owner: codex
  priority: high
  status: ready_for_implementation

objective:
  description: Repair the package so the shipped distribution, CLI, and documentation are internally consistent and usable after the restructure.

scope:
  included:
    - agent_framework
    - scripts
    - README.md
    - pyproject.toml
    - requirements.txt
    - .gitignore
    - docs/package_repair
    - docs/planning
  excluded:
    - .venv
    - unrelated framework plans
    - external CI or publishing configuration not required for package correctness

allowed_paths:
  - agent_framework
  - scripts
  - README.md
  - pyproject.toml
  - requirements.txt
  - .gitignore
  - docs/package_repair
  - docs/planning

dependencies: []
